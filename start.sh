# Root-level start.sh for reference/debugging
# This file is NOT required for Railway deployment when using subdirectory services
# It's provided for reference and local testing purposes

#!/bin/bash

# This script demonstrates how both services could be started if needed
# For Railway.app: Use the backend/start.sh and client/start.sh individually instead

echo "⚠️  This is a reference script. For Railway deployment, use:"
echo "    - backend/start.sh for backend service"
echo "    - client/start.sh for frontend service"
echo ""
echo "For local development, use: docker-compose up"
